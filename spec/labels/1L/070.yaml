spec_version: "1"
plugin:
  name: Label_1L_070
  type: text
  decode_level: PARTIAL
qualifiers:
  labels: ["1L"]
  preambles: ["000000070"]
parse:
  # Pure escape hatch: 9-char prefix strip, 7-field comma split, dep/arr ICAO,
  # message timestamp + eta from HHMMSS, signed-decimal coords via NSEW char,
  # trailing field preserved as remaining.text.
  custom: label_1l_070_decode
formatted:
  description: "Position Report"
  custom: label_1l_070_format
