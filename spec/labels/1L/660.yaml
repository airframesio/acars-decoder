spec_version: "1"
plugin:
  name: Label_1L_660
  type: text
  decode_level: PARTIAL
qualifiers:
  labels: ["1L"]
  preambles: ["000000660"]
parse:
  # Pure escape hatch: 9-char prefix strip, decimal-minutes coords in field 0,
  # field 1 sliced into HHMMSS (timestamp) + FL*100 (altitude) + waypoint
  # (route), remaining fields joined back as remaining.text.
  custom: label_1l_660_decode
formatted:
  description: "Position Report"
  custom: label_1l_660_format
